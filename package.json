{
  "name": "teach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "dev:authoring-api": "turbo dev --filter=authoring-api",
    "dev:authoring-app": "turbo dev --filter=authoring-app",
    "dev:delivery-api": "turbo dev --filter=delivery-api",
    "dev:delivery-app": "turbo dev --filter=delivery-app"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "wait-on": "^9.0.3"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
